plugins {
    id 'base'
}

ext.beamVersion = "2.9.0"
ext.protobufVersion = "3.6.1"

allprojects {
    group = "io.anemos"
    version = "0.0.3-SNAPSHOT"

    repositories {
        mavenLocal()
        jcenter()
    }
}